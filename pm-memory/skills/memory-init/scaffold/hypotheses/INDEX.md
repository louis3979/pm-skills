# Hypotheses — Index

Tracked beliefs, not yet confirmed. See `_SCHEMA.md` for the entry format.

| ID | Statement | Status | Confidence | Decision trigger status |
|---|---|---|---|---|

**Status values**: `proposed` (not yet evidenced) → `active` (has evidence, confidence tracked) → `promoted` (confidence crossed the threshold, became a decision or knowledge entry) or `resolved` (evidence disconfirmed it — kept for the record, not deleted).

This index is maintained by `/hypothesize` (new/updated entries) and `/review` (hygiene: staleness, promotions overdue).
